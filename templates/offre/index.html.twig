{% extends 'base.html.twig' %}

{% block title %}Offre index{% endblock %}

{% block body %}
    <h1>Collaboration</h1>

    <p> Cet espace vous permet de trouver facilement un ou plusieurs partenaires pour vous associer ensemble. 
        Le but: créer de plus gros projets avec des compétences transverses. 
        Vous pourrez échanger, partager, mais aussi gagner en productivité.
    </p>

    <h2>Offre</h2>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description de l'offre</th>
                <th>Delai de livraison</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for offre in offres %}
            <tr>
                <td>{{ offre.nom }}</td>
                <td>{{ offre.sommaire }}</td>
                <td>{{ offre.tempsDeLivraison }}</td>
                <td>
                    <a href="{{ path('offre_show', {'id': offre.id}) }}">show</a>
                    <a href="{{ path('offre_edit', {'id': offre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('offre_new') }}">Create new</a>
{% endblock %}
